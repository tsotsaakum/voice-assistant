Drop `.md`, `.txt`, or `.pdf` files in this folder. Lentswe chunks them and stores a TF-IDF index in `data/rag_index.json` on this computer — no embedding API key.

Open the **Docs** tab and tap **Rebuild index** after you add files (or just ask; the index rebuilds if a file changed). Answers cite the filename. If nothing matches, she says she does not know.
